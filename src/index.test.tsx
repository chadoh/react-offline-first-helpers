import { useCachedUpdater, useSubscribedGetter } from '.'

describe('useCachedUpdater', () => {
  it('is truthy', () => {
    expect(useCachedUpdater).toBeTruthy()
  })
})

describe('useSubscribedGetter', () => {
  it('is truthy', () => {
    expect(useSubscribedGetter).toBeTruthy()
  })
})
